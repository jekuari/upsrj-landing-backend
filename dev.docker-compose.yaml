version: '3.8'

services:
  db:
    image: mongo
    container_name: landing-db
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
    volumes:
      - data:/data/db
    ports:
      - 27017:27017

volumes:
  data:
